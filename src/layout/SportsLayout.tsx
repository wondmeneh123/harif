import Sidebar from "@/web-component/Sidebar";
import React from "react";

const SportsLayout = () => {
  return (
    <div className="flex">
      <Sidebar />
    </div>
  );
};

export default SportsLayout;
